<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="BeeMemoryBank.Desktop.Views.ConfirmForgetDialog"
        Title="Забыть хранилище"
        Width="500"
        Height="260"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#111119"
        Icon="avares://BeeMemoryBank.Desktop/Assets/icon.png">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Подтвердите"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="#F59E0B"
                   Margin="0,0,0,12" />

        <TextBlock Grid.Row="1"
                   Name="BodyText"
                   TextWrapping="Wrap"
                   FontSize="13"
                   Foreground="#D1D5DB" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="10"
                    Margin="0,12,0,0">
            <Button Content="Отмена"
                    Click="OnCancelClick"
                    Padding="20,10"
                    Background="#2D2D3D"
                    Foreground="#FFFFFF"
                    CornerRadius="6" />
            <Button Content="Забыть"
                    Click="OnConfirmClick"
                    Padding="20,10"
                    Background="#F59E0B"
                    Foreground="#111119"
                    FontWeight="SemiBold"
                    CornerRadius="6" />
        </StackPanel>
    </Grid>
</Window>
